import * as actionTypes from './actionTypes';

const initialState={
    dishes:[]
};
export const reducer=(state=initialState,action)=>{
    switch(action.type){
        default:
            return state;
    }
}